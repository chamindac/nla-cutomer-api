apiVersion: v1
kind: Service
metadata:
  name: customerapi-service
spec:
  selector:
    app: customerapi
  type: LoadBalancer
  ports:
    - name: customerapi-port
      port: 8080
      targetPort: 80